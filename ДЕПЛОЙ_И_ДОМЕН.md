# Инструкция по деплою и подключению домена

## 🚀 Деплой сайта на Vercel

### Шаг 1: Подготовка проекта

1. Убедитесь, что все изменения сохранены и проект работает локально:
```bash
npm run build
npm run preview
```

2. Проверьте, что файл `.gitignore` содержит:
```
node_modules
dist
.env
```

### Шаг 2: Создание GitHub репозитория

1. Создайте новый репозиторий на [GitHub.com](https://github.com)
2. Инициализируйте git в проекте (если еще не сделано):
```bash
git init
git add .
git commit -m "Initial commit: 1990 Nails website"
```

3. Подключите удаленный репозиторий:
```bash
git remote add origin https://github.com/ваш-username/1990nails.git
git branch -M main
git push -u origin main
```

### Шаг 3: Деплой на Vercel

#### Вариант A: Через веб-интерфейс

1. Зайдите на [vercel.com](https://vercel.com) и войдите (можно через GitHub)
2. Нажмите **"Add New..."** → **"Project"**
3. Выберите ваш репозиторий `1990nails`
4. Vercel автоматически определит настройки:
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`
5. Нажмите **"Deploy"**
6. Дождитесь завершения деплоя (обычно 1-2 минуты)
7. Ваш сайт будет доступен по адресу: `https://1990nails.vercel.app`

#### Вариант B: Через Vercel CLI

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Войдите в аккаунт:
```bash
vercel login
```

3. Деплой:
```bash
vercel
```

4. Следуйте инструкциям в терминале

### Шаг 4: Подключение собственного домена

#### 4.1. Покупка домена (если еще нет)

Рекомендуемые регистраторы:
- [Namecheap](https://www.namecheap.com)
- [GoDaddy](https://www.godaddy.com)
- [Google Domains](https://domains.google)

#### 4.2. Добавление домена в Vercel

1. В панели Vercel откройте ваш проект
2. Перейдите в **Settings** → **Domains**
3. Введите ваш домен (например: `1990nails.com` или `www.1990nails.com`)
4. Нажмите **"Add"**
5. Vercel покажет вам DNS записи, которые нужно добавить

#### 4.3. Настройка DNS записей

**Вариант 1: Использование Vercel DNS (рекомендуется)**

1. В Vercel выберите **"Use Vercel DNS"**
2. Vercel предоставит вам nameservers (например):
   - `ns1.vercel-dns.com`
   - `ns2.vercel-dns.com`
3. Перейдите в панель управления вашего регистратора домена
4. Найдите раздел **"DNS"** или **"Nameservers"**
5. Замените текущие nameservers на те, что предоставил Vercel
6. Сохраните изменения

**Вариант 2: Использование A/CNAME записей**

Если вы хотите оставить текущие nameservers:

1. В Vercel выберите **"Configure DNS"**
2. Добавьте следующие записи в панели вашего регистратора:

   **Для основного домена (1990nails.com):**
   ```
   Type: A
   Name: @
   Value: 76.76.21.21
   TTL: 3600
   ```

   **Для www поддомена (www.1990nails.com):**
   ```
   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   TTL: 3600
   ```

3. Сохраните изменения

#### 4.4. Ожидание распространения DNS

- Изменения DNS обычно вступают в силу через **15 минут - 48 часов**
- Проверить статус можно через [whatsmydns.net](https://www.whatsmydns.net)

#### 4.5. SSL сертификат

- Vercel автоматически выдает бесплатный SSL сертификат (HTTPS)
- Активация происходит автоматически после настройки DNS
- Обычно занимает несколько минут

## 🔧 Дополнительные настройки

### Настройка редиректов

В корне проекта создайте файл `vercel.json`:

```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

Это необходимо для правильной работы React Router (если используется) и SPA.

### Переменные окружения

Если нужны переменные окружения:
1. В Vercel: **Settings** → **Environment Variables**
2. Добавьте переменные (например, API ключи)
3. Пересоберите проект

### Автоматический деплой

- При каждом `git push` в main ветку Vercel автоматически пересобирает и деплоит сайт
- Можно настроить деплой только из определенных веток в настройках проекта

## 📋 Чек-лист перед деплоем

- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Все изображения загружены в `public/`
- [ ] Логотип находится в `public/logo.png`
- [ ] Все контактные данные обновлены
- [ ] Google Maps URL обновлен
- [ ] Проект загружен в GitHub
- [ ] Vercel проект создан и подключен к репозиторию
- [ ] Домен добавлен в Vercel
- [ ] DNS записи настроены
- [ ] SSL сертификат активирован

## 🆘 Решение проблем

### Сайт не открывается после деплоя
- Проверьте логи в Vercel Dashboard → Deployments
- Убедитесь, что `npm run build` выполняется успешно

### Домен не работает
- Проверьте DNS записи через [whatsmydns.net](https://www.whatsmydns.net)
- Убедитесь, что прошло достаточно времени (до 48 часов)
- Проверьте настройки в панели регистратора домена

### Изображения не загружаются
- Убедитесь, что все файлы в `public/` закоммичены в git
- Проверьте пути к изображениям (должны начинаться с `/`)

## 📞 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel DNS Guide](https://vercel.com/docs/concepts/projects/domains)
- [GitHub](https://github.com)

---

**Готово!** После выполнения всех шагов ваш сайт будет доступен по вашему домену. 🎉
